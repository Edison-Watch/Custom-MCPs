name: TS Fleet Servers

on:
  push:
    branches:
      - '**'
    paths:
      - 'servers/**'
      - 'shared/**'
      - '.github/workflows/servers_ts.yaml'
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'servers/**'
      - 'shared/**'
      - '.github/workflows/servers_ts.yaml'
  workflow_dispatch:

jobs:
  test:
    name: ${{ matrix.server }} (typecheck + test)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        server:
          - image-host
          - reddit
          - youtube
          - x-scraper
          - linkedin-scraper
    defaults:
      run:
        working-directory: servers/${{ matrix.server }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile || bun install

      - name: Unit tests (offline pure logic)
        run: bun run test

      - name: Typecheck
        run: bun run typecheck

      - name: Integration tests (workerd + real R2/DO bindings)
        run: bun run test:integration

  # stdio servers are npm-published local processes (no Cloudflare Worker,
  # no workerd integration). They build with tsc and unit-test with vitest,
  # so they get their own job instead of the Worker matrix above.
  test-stdio:
    name: ${{ matrix.server }} (stdio · typecheck + test + build)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        server:
          - adb
    defaults:
      run:
        working-directory: servers/${{ matrix.server }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Unit tests (offline pure logic)
        run: bun run test

      - name: Typecheck
        run: bun run typecheck

      - name: Build (tsc -> dist)
        run: bun run build
