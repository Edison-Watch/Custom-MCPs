{
  "name": "@edison/mcp-image-host",
  "version": "0.1.0",
  "private": true,
  "description": "Edison first-party MCP server: upload an image, get a public non-expiring URL. Cloudflare Worker + R2.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0",
    "agents": "^0.0.109",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "@types/bun": "^1.3.14",
    "typescript": "^5.7.3",
    "wrangler": "^4.20.0"
  }
}
